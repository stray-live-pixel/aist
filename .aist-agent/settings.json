{
  "activeInstructionRefs": [
    {
      "scope": "global",
      "id": "правила-разработки"
    }
  ],
  "instructions": [
    {
      "id": "aist-coder",
      "label": "aist-coder",
      "content": "# Agent notes\n\n- Если работаешь в автономном прогоне (`scripts/run-tasks.sh`) — обязательно прочитай [AUTONOMOUS-WORKFLOW.md](AUTONOMOUS-WORKFLOW.md) и следуй ему буквально (фазы implement/review, обязательные unit + US + e2e TC, никаких вопросов пользователю, коммит после каждой фазы, переименование в `DONE-` только в фазе review).\n\n## Комментарии в коде\n\nКод пишет ИИ-агент — без подробных комментариев человек не сможет ревьюить. Это **переопределяет** дефолтное «не пишем комментарии».\n\n- **Язык** — русский. Устоявшиеся термины (webview, publisher, lint-staged) не переводим.\n- **Покрывай** функции/методы/компоненты, нетривиальные блоки, поля значимых типов, конфиги (через комментарии в соседних файлах, если формат не поддерживает inline). Формат — JSDoc над объявлениями, `//` внутри.\n- **Объясняй «почему»**, а не только «что»: альтернативы, инварианты, граничные случаи. Не дублируй имя функции словами.\n- **Обновляй** при правке кода — устаревший комментарий хуже отсутствующего.\n\n## Чистота и декомпозиция\n\n- **Размер.** ~300 строк — не растим, режем; 500+ — явный сигнал. Один файл = одна осмысленная единица.\n- **Имена.** Никаких 1-2-буквенных переменных (`e`, `ev`, `ctx`). Исключения: `i`/`j` в счётчиках, `_` для unused. Везде остальное — `event`, `context`, `message`.\n- **Декомпозиция функций.** Длинная функция со смысловыми этапами → именованные хелперы (имя — «что», тело — «как»). Но не плодим файлы на 5 строк ради чистоты.\n\n## Архитектура\n\n### Хранилище ранов: `.agents/`\n\nАртефакты живут в `.agents/runs/<runId>/` **в корне workspace пользователя** — намеренно, чтобы коммитились вместе с кодом. globalStorage/БД отброшены: отрывают контекст от проекта.\n\n- `meta.json` — атомарная перезапись через temp+rename. `chat.jsonl` — append-only.\n- Структуру правит только [src/extension/entities/run/storage.ts](src/extension/entities/run/storage.ts). Новый файл артефакта (`plan.md`, `decisions.md`) — туда же, не размазываем fs по сервисам.\n- Без открытого workspace ран не создать — `RunStorageError`, не fallback.\n\n### Layout extension'а\n\nESLint-границы трактуют `src/extension/` как единый элемент, но раскладку держим FSD:\n\n- `shared/` — инфраструктура без знаний о домене.\n- `entities/` — предметная модель.\n- `features/` — пользовательские сценарии.\n- `providers/` — webview-провайдеры VS Code; должны быть тонкими, бизнес-логику сюда не кладём.\n\n### IPC webview ↔ extension\n\nИсточник правды — дискриминированные union'ы в [src/extension/features/run-management/messages.ts](src/extension/features/run-management/messages.ts). Дублируются в `src/webview/shared/runs/` (ESLint запрещает кросс-импорт extension↔webview, чтобы Node-код не утёк в браузерный бандл). Правишь контракт — синхронно правишь обе стороны; разъезд поймает TS на билде.\n\nМодель событийная (без request-id/Promise): один запрос → может породить несколько событий. `requestId` добавим, когда понадобится корреляция, не раньше.\n\n## Стили webview'а: токены и Tailwind\n\nС #0015 webview оформляется через семантические токены поверх vscode-переменных + Tailwind 4 (CSS-first).\n\n- **Источник токенов** — [src/webview/app/app.css](src/webview/app/app.css). Цвета — только через `var(--vscode-*)`, иначе ломается переключение light/dark/high-contrast. Новый цвет добавляешь там же: семантическое имя (`bg-surface`, не `bg-zinc-900`) → маппинг на ближайшую vscode-переменную (с fallback'ом через запятую внутри `var()`).\n- **Сборка CSS** — `npm run build:webview-css` (через `@tailwindcss/cli`, выход — `out/webview/app.css`). Подключается раньше `media/main.css`, поэтому unlayered ad-hoc-стили из `media/main.css` имеют приоритет над layered Tailwind'ом. Это даёт плавную миграцию: новые компоненты идут на токены, старые работают как есть, переписываем итеративно (#0016+).\n- **В компонентах** используем utility-классы Tailwind (`inline-flex`, `gap-1`, `border`) и токенные классы (`bg-surface`, `text-foreground`, `border-border-focus`). Inline-стили и точечные цвета — нет: они ломают темы.\n- **Иконки** — `lucide-react` (а не emoji). Размер задаём через `size={N}`, цвет наследуется (`currentColor`) — попадает под токен `text-*`.\n- **Условная сборка классов** — `clsx` (уже в зависимостях). `tailwind-merge` сейчас не нужен: конфликтов классов нет.\n- **CSS modules** — допустимы для «нестандартных» компонентов со сложной версткой/состояниями (`&:hover .child`, анимации, сетки), где Tailwind становится нечитаемым. esbuild ≥0.24 поддерживает `*.module.css` нативно (loader `local-css`), Vite не нужен. Пока инфраструктура не подключена — первый компонент, которому это понадобится, поднимает её сам: добавляет `<link>` на `out/webview/main.css` в [html.ts](src/extension/webview/html.ts) (esbuild эмитит CSS-бандл рядом с `main.js`, как только появится первый импорт `.module.css`) и `declare module '*.module.css'` в `src/webview/global.d.ts` для типов. Базовые токены и spacing внутри модулей — через `var(--color-*)` или `@apply`, чтобы темы продолжали переключаться."
    }
  ],
  "activeModeRef": {
    "scope": "global",
    "id": "coder"
  },
  "presets": [
    {
      "id": "ai-coder",
      "label": "ai-coder",
      "instructionRefs": [
        {
          "scope": "global",
          "id": "правила-разработки"
        }
      ],
      "modeRef": {
        "scope": "global",
        "id": "coder"
      },
      "scope": "local"
    }
  ],
  "model": "codex:gpt-5.5",
  "maxToolIterations": 600,
  "reasoningEffort": "high",
  "codexServiceTier": "priority",
  "streamingEnabled": false,
  "language": "ru",
  "toolPermissions": {
    "get_workspace_info": "auto",
    "list_files": "auto",
    "read_file": "auto",
    "read_file_range": "auto",
    "outline_file": "auto",
    "grep_search": "auto",
    "run_bash_script": "auto",
    "write_file": "auto",
    "replace_in_file": "auto",
    "create_directory": "auto",
    "delete_path": "auto",
    "create_plan": "auto",
    "update_plan": "auto",
    "set_plan_item_status": "auto",
    "run_skill": "auto"
  },
  "projectToolDisabledIds": [],
  "auxiliaryModels": {
    "compaction": {
      "model": "",
      "reasoningEffort": "auto",
      "allowTools": false
    },
    "tool": {
      "model": "codex:gpt-5.3-codex-spark",
      "reasoningEffort": "medium",
      "allowTools": false,
      "overrides": []
    }
  },
  "customSkills": [
    {
      "id": "webview-design-system",
      "label": "Webview design system",
      "description": "Load this before any task that creates, edits, reviews, or refactors AIST webview UI, including src/webview, Composer, shared/ui components, SCSS Modules, Storybook stories, settings pages, chat pages, message cards, forms, modals, buttons, selects, badges, or layout styling.",
      "command": "cat docs/webview-design-system.md",
      "permission": "auto"
    },
    {
      "id": "aist-e2e-testing",
      "label": "AIST e2e testing guide",
      "description": "Load this before creating, editing, reviewing, debugging, or updating AIST Playwright e2e tests, VS Code webview user flows, OpenRouter mock scenarios, and screenshot snapshots.",
      "command": "cat docs/e2e-testing-skill.md",
      "permission": "auto"
    }
  ],
  "providerProfiles": [
    {
      "id": "openrouter",
      "name": "OpenRouter",
      "provider": "openrouter",
      "endpoint": "https://openrouter.ai/api/v1/chat/completions",
      "proxyHost": "",
      "builtIn": true
    },
    {
      "id": "codex",
      "name": "ChatGPT Codex",
      "provider": "codex",
      "endpoint": "https://chatgpt.com/backend-api/codex/responses",
      "proxyHost": "",
      "builtIn": true
    }
  ],
  "activePresetId": "ai-coder",
  "compaction": {
    "enabled": true,
    "thresholdPercent": 70,
    "keepLastMessages": 0,
    "model": "codex:gpt-5.3-codex-spark",
    "reasoningEffort": "auto",
    "allowTools": false
  }
}
