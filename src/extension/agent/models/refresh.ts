/**
 * Время жизни кеша списка моделей.
 *
 * Список моделей не нужно запрашивать при каждом открытии webview: он меняется
 * редко, а частые запросы ухудшают UX при старте расширения.
 */
export const MODEL_LIST_CACHE_TTL_MS = 5 * 60 * 1000;
