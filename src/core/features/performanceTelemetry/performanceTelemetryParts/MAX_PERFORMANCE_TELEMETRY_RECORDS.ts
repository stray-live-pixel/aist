/**
 * Что это: лимит последних performance records.
 * Зачем нужно: telemetry должна помогать искать лаги, а не сама превращаться в нагрузку на FS/CPU.
 * Какую продуктовую проблему решает: расширение остаётся быстрым даже после долгой работы на компьютере.
 */
export const MAX_PERFORMANCE_TELEMETRY_RECORDS = 500;
