/**
 * Что это: поддерживаемый формат экспорта автономной сессии.
 * Зачем нужно: CLI и daemon используют единый набор значений для export-команды.
 * Какую продуктовую проблему решает: пользователь может получить отчёт в markdown или машинно-читаемом JSON.
 */
export type AutonomousExportFormat = 'markdown' | 'json';
