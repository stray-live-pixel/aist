/**
 * Что это: вес полезности заметки по умолчанию.
 * Зачем нужно: старые заметки без веса продолжают работать и участвуют в сортировке памяти.
 * Какую продуктовую проблему решает: миграция памяти проходит незаметно для пользователя.
 */
export const DEFAULT_MEMORY_IMPORTANCE = 50;
