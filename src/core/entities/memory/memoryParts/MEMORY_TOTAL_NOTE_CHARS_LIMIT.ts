/**
 * Что это: общий лимит текста всех заметок памяти.
 * Зачем нужно: память остаётся быстрой и минималистичной, а не превращается во второй чат-лог.
 * Какую продуктовую проблему решает: агент получает только важные правила без раздувания контекста.
 */
export const MEMORY_TOTAL_NOTE_CHARS_LIMIT = 50_000;
