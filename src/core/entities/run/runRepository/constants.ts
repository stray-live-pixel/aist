/**
 * Что это: версия файловой схемы run-хранилища.
 * Зачем нужно: все записи run получают одинаковую schemaVersion.
 * Какую проблему решает: будущие миграции смогут отличать формат данных без поиска magic number.
 */
export const RUN_SCHEMA_VERSION = 1;
