/**
 * Что это: минимальный runtime-контекст файлового репозитория чатов.
 * Зачем нужно: сценарные функции получают единый источник пути, времени и генерации id.
 * Какую продуктовую проблему решает: после декомпозиции нет разных источников правды для хранения чата.
 */
export type ChatRepositoryContext = {
  rootPath: string;
  idFactory: () => string;
  now: () => number;
};
