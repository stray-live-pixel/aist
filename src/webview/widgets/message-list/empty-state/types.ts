/**
 * Что это: props пустого состояния истории.
 * Зачем нужно: отдельный тип оставляет место для будущих вариантов без смены публичного импорта.
 */
export type EmptyStateProps = Record<string, never>;
