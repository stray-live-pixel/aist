# AIST isolated run result

Session: 0d36a45e-af83-4bc3-a79a-4d7ea0b64742
Updated at: 2026-06-03

## Summary

- Reworked isolated agents toward an autonomous Docker model: the container no longer receives a host worktree bind mount, but clones the GitHub repository itself and creates the target branch inside the container.
- Added container bootstrap: install git/gh, clone repo, checkout base ref, create task branch, run `npm ci`, build/link AIST CLI.
- Changed isolated agent execution to run `aist chat ask --jsonl` inside the container and stream runtime events back into the local daemon chat.
- Added local chat event import so VS Code can observe remote/container work through persisted local chat state and daemon events.
- Moved commit/push/PR finalization into the container, keeping host responsibility limited to session state, event log, and container lifecycle.
- Added regression coverage for rebasing container runtime events into the local chat.

## Verification

- Attempted `npm run typecheck`, but it could not run because dependencies are not installed in the container: `tsc: not found`.
- Attempted focused Vitest for `applyRuntimeEventToChat.test.ts`, but it could not run because dependencies are not installed in the container: `vitest: not found`.
- Attempted `npx tsc --noEmit --pretty false`, but it fetched unsupported `tsc@2.0.4` because the project TypeScript package is not installed.

## Notes

- The current implementation still uses the local Docker CLI as the transport, but the execution contract is now remote-ready: repo, agent CLI, branch, changes, commit, push, and PR are all inside the container.
- Chat state is obtained on the computer by treating the container as a JSONL runtime-event producer: the local daemon imports those events into its normal chat repository and broadcasts standard webview patches.
