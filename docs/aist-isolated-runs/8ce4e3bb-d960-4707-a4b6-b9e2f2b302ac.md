# Isolated run 8ce4e3bb-d960-4707-a4b6-b9e2f2b302ac

## Summary

- Made the standard ChatPage aware of isolated Docker chats: finished-session follow-up prompts are routed to `isolation.continue`, and stop uses `isolation.stop` while the session is active.
- Added a Docker-session notice above the standard Composer so users can see that the local-looking chat controls an isolated agent.
- Updated the Isolated agents page to present itself as launcher/log diagnostics and added a clear “Open standard chat” action for each session.
- Changed bridge full-state refresh preservation so isolated lifecycle refreshes do not unexpectedly switch the active chat; users open the live Docker chat explicitly.
- Added regression coverage for active-chat preservation and extended isolated-chat lookup coverage.

## Verification

- `npx vitest run src/extension/agent/daemon/bridge/getPreservedBridgeActiveChatId.test.ts src/webview/shared/lib/isolation/findIsolationSessionByChatId.test.ts` — failed before running tests because dependencies are not installed in the container (`Cannot find module 'vitest/config'`).
- `npm run typecheck` — failed because `tsc` is not available in the container (`sh: 1: tsc: not found`).
