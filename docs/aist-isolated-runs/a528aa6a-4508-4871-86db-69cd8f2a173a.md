# Isolated run a528aa6a-4508-4871-86db-69cd8f2a173a

## Задача

Перенести кнопку `Isolated Agents` из Composer в системное меню, потому что в Composer уже слишком много кнопок.

## Изменения

- Убрана кнопка `Isolated agents` из `FloatingChatActions`, то есть из правой группы действий Composer.
- Удалён проп `onOpenIsolation` из использования `FloatingChatActions` на странице чата и из regression-теста.
- Regression-тест порядка кнопок Composer обновлён: теперь он проверяет, что Composer содержит только chat-local действия.
- Системный путь открытия сохранён: команда `openrouterAgent.openIsolation` уже зарегистрирована в `package.json` в `view/title` и открывает модалку через событие `showIsolation`.

## Проверка

- `npm run test -- src/webview/pages/chat/FloatingChatActions.test.tsx` — не выполнен: в окружении отсутствует бинарь `vitest` (`sh: 1: vitest: not found`).
- `npm run typecheck` — не выполнен: в окружении отсутствует бинарь `tsc` (`sh: 1: tsc: not found`).
