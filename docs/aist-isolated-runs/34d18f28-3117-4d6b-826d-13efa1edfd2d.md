# Isolated run 34d18f28-3117-4d6b-826d-13efa1edfd2d

## Summary

Implemented remote SSH runner support for isolated agents alongside local Docker:

- Added global remote-server persistence under the user AIST root, not the repository.
- Added daemon RPC methods for listing runners, and listing/upserting/deleting remote SSH servers.
- Updated isolated session lifecycle to resolve either Local Docker or a Remote SSH execution provider, prevent concurrent use of one remote server, and run bootstrap/tools/finalization through the selected provider.
- Added VS Code bridge, webview state, IPC actions and message types for remote server settings and runner selection.
- Extended the Isolated Agents page with a runner selector, availability preview, and a global remote server settings card using passwordless SSH (ssh-agent or private key path) plus GitHub auth mode.

## Verification

Attempted to run `npm run typecheck`, but the command could not execute in this isolated environment because the task container was missing:

```text
Error response from daemon: No such container: aist-isolated-34d18f28-3117-4d6b-826d-13efa1edfd2d
```

No full typecheck result is available from this run.
