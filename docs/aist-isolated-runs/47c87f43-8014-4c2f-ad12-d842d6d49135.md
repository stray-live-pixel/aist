# AIST isolated run result

Session: 47c87f43-8014-4c2f-ad12-d842d6d49135

## User task

В Isolated agents после создания ПР надо удалять/закрывать Worktree, чтобы в системе не оставалось лишних следов работы агента.

## Summary

- Added automatic isolated worktree cleanup after successful finalization and PR metadata collection.
- The final `ready_for_review` session keeps commit/PR fields, but clears `worktreePath`, `containerId`, and `containerName` so UI/state no longer points to stale local resources.
- Added a regression test that verifies the manager removes the worktree after a PR-ready run.

## Verification

- `npm test -- src/cli/daemonServer/isolation/IsolationSessionManager.test.ts` — failed because `vitest` is not installed in this isolated worktree (`sh: 1: vitest: not found`).
- `npx tsc --noEmit --pretty false` — failed because local TypeScript is not installed and `npx` resolved deprecated `tsc@2.0.4` instead of the project compiler.
- `git diff --check` — failed because this isolated worktree's `.git` pointer references a missing original worktree gitdir.
