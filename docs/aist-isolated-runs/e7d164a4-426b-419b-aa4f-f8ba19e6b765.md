# Isolated run e7d164a4-426b-419b-aa4f-f8ba19e6b765

## Summary

- Added isolated-agent prompt rules that require every daemon-finalized PR to stay small, simple, complete, and easy for a human to review.
- Added guidance to decompose complex or iterative work into simple finished subtasks, preferably parallel isolated-agent PRs when independent and sequential small PRs otherwise.
- Extracted the isolated prompt builder into a focused helper and added regression coverage for the new PR-size/decomposition rule.

## Verification

- `npx vitest run src/cli/daemonServer/methods/runIsolationAgent.test.ts` — failed before running tests because dependencies are not installed in the container (`Cannot find module 'vitest/config'`).
