# Isolated run e7d164a4-426b-419b-aa4f-f8ba19e6b765

## Summary

- Tightened isolated-agent rules so a large/complex user task is decomposed into ordered subtasks and then executed iteratively in a loop: start, inspect/edit/verify, commit, mark done, continue.
- Kept the reviewability rule that every changed subtask must call `create_isolation_commit` before the next subtask starts, so each task maps to a separate checkpoint commit.
- Enabled `spawn_agent` inside isolated runs even when no auxiliary tool model is configured, and added an isolated adapter that supports parallel-safe research without applying changes outside the main worktree.
- Updated regression coverage for the new loop/commit prompt rules and the isolated `spawn_agent` execution path.

## Verification

- `npm install --ignore-scripts` — installed project dependencies in the isolated container so focused tests and typecheck can run.
- `npx vitest run src/cli/daemonServer/methods/runIsolationAgent.test.ts src/cli/daemonServer/isolation/runtime/auxiliaryModelSpawnAgent.test.ts` — passed.
- `npm run typecheck` — passed.
